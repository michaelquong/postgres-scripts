postgres:
  storage:
    local: ./backups
  source:
    username: postgres
    password: postgres
    hostname: localhost
    port: 5432
    database: example
  target:
    username: postgres
    password: postgres
    hostname: localhost
    port: 5433
    database: example
  
